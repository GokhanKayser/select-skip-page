const ChooseDatePage = ({ onNext }) => {
  return (
    <section>
      <h1>Choose Date Page</h1>
      <button onClick={onNext}>Continue</button>
    </section>
  );
};

export default ChooseDatePage;
