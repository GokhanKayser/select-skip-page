const PermitCheckPage = ({ onNext }) => {
  return (
    <section>
      <h1>Permit Check Page</h1>
      <button onClick={onNext}>Continue</button>
    </section>
  );
};

export default PermitCheckPage;
